# 更新记录（Changelog）

本文件集中记录 Vid Folder Browser 各版本更新内容。最新版本在最上方。

---

## v0.7.0 — 列表悬停预览（2026-08-23）

### 列表视图悬停预览（v0.7）

1. **列表也能悬停预览**：列表视图中鼠标悬停行约 0.4 秒后，在行下方浮出横向胶片预览条（8 帧一排），鼠标水平移动切换帧并高亮当前帧，角标实时显示帧时间；移出约 200ms 后自动关闭。
   - 完全复用 v0.6 主进程的抽帧、缓存、队列、LRU 与取消机制，主进程预览链路零改动。
   - 网格与列表共用同一预览会话，天然互斥：同一时刻只有一个预览可见。
   - 滚动、键盘导航、排序/筛选/切换文件夹时预览条立即关闭并取消未开始请求；列表行新增小缩略图，作为预览的载体。
   - 缺少 ffmpeg 时与网格一致：开关禁用并提示。

> 说明：v0.7 原规划的"播放进度记忆"经评估对浏览场景价值有限，已按用户要求移除，不进入本版本（决策记录见 `PROJECT_MEMORY.md`）。

### 已知限制（v0.7）

- 依赖本机 `ffmpeg` 与 `ffprobe`，不内置、不自动下载；缺失时仍可浏览已有缓存，悬停预览自动禁用。
- 缓存失效判断基于"路径 + 大小 + 修改时间"；三者均不变但内容变化时可能命中旧缓存（已接受的边界）。
- 首次扫描需逐视频读取元信息并生成封面，耗时与视频数量正相关；二次扫描（热缓存）明显更快。
- 不提供实时文件监控，文件夹内容变化后需手动刷新。

---

## v0.6.0 — 悬停多帧预览（2026-08-21）

> 自 v0.3.0 后连续三阶段（v0.4 质量加固 / v0.5 界面体系 / v0.6 悬停预览）的集大成版本。

### 悬停多帧预览（v0.6）

1. **悬停即看**：网格视图中鼠标悬停封面约 0.4 秒后，自动沿时间轴播放该视频的 8 个代表帧，帮助快速判断内容；鼠标水平移动切换帧，角标显示当前帧时间；离开后立即恢复主封面。
   - 一次调用生成全部帧，冷生成平均约 168ms；帧缓存命中仅约 7ms。
   - 仅网格视图启用；工具栏可随时开关（状态持久化），未检测到 ffmpeg 时自动禁用并提示。
2. **按需生成，零预扫负担**：仅在悬停确认需要时才生成帧，不影响首次扫描速度；不足 1 秒的视频自动减少帧数；生成失败自动回退静态封面，不影响浏览。
3. **独立缓存与容量治理**：帧缓存与主封面缓存完全独立，上限 512 MiB，超出后按近似 LRU 整组淘汰；单组帧缺失或损坏时自动整组重建。
4. **响应式调度**：生成队列并发 1、排队上限 8，同视频请求自动合并；快速移开即取消，运行中的生成结果不会串位；退出应用时安全清理后台进程。

### 新界面与组件体系（v0.5）

5. **统一组件体系**：接入 shadcn/ui（Tailwind CSS + Radix），工具栏、下拉、菜单、提示、滚动、弹层等基础组件全部统一；整体重新设计为紧凑、克制的工作导向暗色界面。
6. **层级文件夹树**：左侧按真实目录层级展示视频文件夹，替代原扁平列表；显示直接视频数与累计视频数；支持展开/折叠、名称过滤、一键全部折叠、右键"在资源管理器中打开"、完整键盘导航。
7. **快速访问**：自动记录最近成功打开的文件夹（最多 10 条），可固定常用文件夹（固定项不淘汰）；失效路径标记提示、可移除记录；支持从资源管理器拖入单个文件夹打开。
8. **高频浏览与布局**：双击用默认播放器打开；网格/列表方向键移动、`Enter` 打开、`Ctrl+C` 复制路径、详情面板"在文件夹树中定位"；左侧栏可折叠、拖拽调宽，右侧详情可收起，窄窗口自动切换侧滑覆盖层；布局与展开状态持久化。

### 质量加固（v0.4）

9. **错误提示统一**：全部可预期错误按类别展示，界面显示简洁中文说明，技术详情仅在悬停时可见。
10. **元信息与封面任务解耦**：ffprobe 失败不阻断封面生成，ffmpeg 失败不丢失已读取的时长与分辨率。
11. **扫描错误可见**：根目录不可读明确报错；子目录不可读跳过并给出警告摘要；取消/为空/出错三态明确区分。
12. **缓存可靠性**：缺失或损坏的缩略图自动重新生成；元信息缓存损坏时自动备份并安全回退；扫描后只清理应用自身缓存内的失效记录与孤儿缩略图，不触碰用户视频。

### 已知限制（v0.6）

- 依赖本机 `ffmpeg` 与 `ffprobe`，不内置、不自动下载；缺失时仍可浏览已有缓存，悬停多帧预览自动禁用。
- 缓存失效判断基于"路径 + 大小 + 修改时间"；三者均不变但内容变化时可能命中旧缓存（已接受的边界）。
- 首次扫描需逐视频读取元信息并生成封面，耗时与视频数量正相关；二次扫描（热缓存）明显更快。
- 不提供实时文件监控，文件夹内容变化后需手动刷新。

---

## v0.5.0 — 界面体系、文件夹树与快速访问（2026-08-13 发布）

> 自 v0.3.0 后的首个新版本：包含 v0.4（发布质量加固）与 v0.5（界面体系、文件夹树与快速访问）两阶段改动。

### 新界面与组件体系

1. **统一组件体系**：接入 shadcn/ui（Tailwind CSS + Radix），工具栏、下拉、菜单、提示、滚动、弹层等基础组件全部统一；整体重新设计为紧凑、克制的工作导向暗色界面，长文件名、长路径不再溢出控件。
2. **层级文件夹树**：左侧按真实目录层级展示视频文件夹，替代原扁平列表；每个文件夹显示直接视频数与包含子文件夹的累计视频数；支持展开/折叠（与选择互不误触）、按名称过滤、一键全部折叠、右键"在资源管理器中打开"；完整键盘导航（方向键移动、左右展开/折叠、Enter 选择），选中节点自动展开祖先并保持可见。
3. **快速访问**：自动记录最近成功打开的文件夹（最多 10 条，按时间排序、路径去重），可固定常用文件夹（固定项不淘汰）；失效路径标记提示、可随时移除记录，不影响启动、不触碰文件系统；从资源管理器直接拖入单个文件夹即可打开。
4. **高频浏览与布局**：双击卡片/行用默认播放器打开；网格/列表内方向键移动选择；`Enter` 打开；`Ctrl+C` 复制路径；详情面板支持"在文件夹树中定位"；左侧栏可折叠并可拖拽调宽，右侧详情面板可收起；窄窗口下详情自动切换为侧滑覆盖层；树展开状态、侧栏与详情栏状态持久化，重启后恢复。

### 质量加固（v0.4）

5. **错误提示统一**：全部可预期错误按类别展示，界面显示简洁中文说明，技术详情仅在悬停时可见。
6. **元信息与封面任务解耦**：ffprobe 失败不阻断封面生成，ffmpeg 失败不丢失已读取的时长与分辨率。
7. **扫描错误可见**：根目录不可读明确报错；子目录不可读跳过并给出警告摘要；取消/为空/出错三态明确区分。
8. **缓存可靠性**：缺失或损坏的缩略图自动重新生成；元信息缓存损坏时自动备份并安全回退；扫描完成后只清理应用自身缓存内的失效记录与孤儿缩略图，不触碰用户视频。

### 已知限制

- 依赖本机 `ffmpeg` 与 `ffprobe`，不内置、不自动下载；缺失时仍可浏览已有缓存。
- 缓存失效判断基于"路径 + 大小 + 修改时间"；三者均不变但内容变化时可能命中旧缓存（已接受的边界）。
- 首次扫描需逐视频读取元信息并生成封面，耗时与视频数量正相关；二次扫描（热缓存）明显更快。
- 不提供实时文件监控，文件夹内容变化后需手动刷新。

---

## v0.4.0 — 发布质量加固（2026-08-10 发布）

> 不新增浏览功能，将应用从"功能可用"推进到"行为可解释、质量可验证、发布可重复"。

1. **错误提示统一**：全部可预期错误按类别展示（依赖缺失、目录不可读、文件不可读、元信息读取失败、封面生成失败、缓存损坏等）；界面显示简洁中文说明，技术详情（原始 stderr）仅在悬停时可见；单个视频的元信息失败与封面失败分开显示，不再互相覆盖。
2. **元信息与封面任务解耦**：ffprobe 失败不阻断可执行的封面生成；ffmpeg 失败不丢失已读取的时长与分辨率；右键"重新生成封面"只重试必要阶段，不破坏仍有效的元信息。
3. **扫描错误可见**：根目录不可读时明确提示扫描失败，不再误报为"扫描完成"或"没有视频"；子目录不可读时跳过并给出可查看的警告摘要；取消扫描、目录为空、目录不可读三态明确区分。
4. **缓存可靠性**：缩略图缺失、零字节或损坏时自动降级并重新生成，应用不崩溃；元信息缓存损坏时自动备份原文件并安全回退；扫描完成后自动清理失效记录与孤儿缩略图，仅操作应用自身缓存目录，不触碰用户视频文件。
5. **构建与发布**：安装包命名统一为 `Vid.Folder.Browser-<版本>-win-x64.zip`；新增四份质量文档（性能基准指南、缓存失效验证记录、发布冒烟清单、发布流程清单）。

### 已知限制

- 依赖本机 `ffmpeg` 与 `ffprobe`，不内置、不自动下载。
- 缓存失效判断基于"路径 + 文件大小 + 修改时间"；三者均不变但内容变化时可能命中旧缓存（明确接受的已知边界）。
- 首次扫描需为每个视频读取元信息并生成封面，耗时与视频数量正相关；二次扫描（热缓存）明显更快。

---

## v0.3.0 — 浏览效率与 ffmpeg 可用性增强（2026-08-10 发布）

1. **设置持久化**：记住视图模式（网格 / 列表）、排序字段与升降序、缩略图大小；设置写入现有 `settings.json`，不引入数据库。
2. **左侧目录数量**：从"直接包含视频数"改为"目录及所有子目录累计视频数"。
3. **列表视图表头排序**：支持文件名、时长、大小、修改时间排序，当前排序方向在表头显示。
4. **ffmpeg 可用性检测**：启动时自动检测 `ffmpeg` 与 `ffprobe` 是否可执行；顶部提示缺失依赖，状态栏显示状态。
5. **缺失依赖不崩溃**：缺少依赖时应用仍可打开、扫描、展示已有缓存。

---

## v0.1.0 — 首个版本（2026-06-12 发布）

基础能力首发：

- Electron + React + TypeScript + Vite 项目结构
- 选择本地文件夹并记住上次路径，启动自动恢复
- 递归扫描 `.mp4` `.mkv` `.avi` `.mov` `.wmv` `.flv` `.webm` `.m4v`
- 扫描过程增量推送结果，不等待全部完成
- `ffprobe` 读取时长、分辨率；`ffmpeg` 生成 16:9 封面并缓存到应用数据目录
- 缓存基于路径 + 大小 + 修改时间失效
- 文件名搜索、排序、升降序、缩略图大小三档
- 右键菜单：打开所在目录、默认播放器打开、复制完整路径、重新生成封面
- 右侧详情面板与底部状态栏
