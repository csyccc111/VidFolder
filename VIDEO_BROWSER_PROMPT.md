# 视频文件浏览器开发提示词

你是一名资深桌面应用开发工程师。请为我开发一个轻量级桌面程序，定位是“带封面预览的视频文件夹浏览器”，不是播放器，也不是复杂素材管理器。

## 项目目标

开发一个小型桌面应用，让用户选择本地文件夹后，可以看到该文件夹及其子文件夹中的所有视频文件，并以网格形式展示视频封面、文件名、大小、时长。用户可以搜索、排序、刷新，并通过右键菜单快速打开视频所在目录或用系统默认播放器打开视频。

## 推荐技术栈

- 优先使用 Electron + React + TypeScript。
- 可以使用 Vite 作为前端构建工具。
- 使用 Node/Electron 主进程访问文件系统。
- 使用 ffmpeg/ffprobe 获取视频封面、时长、分辨率等信息。
- 如果项目中已有技术栈，请优先沿用已有技术栈，不要无谓重构。

## 核心功能要求

### 1. 选择文件夹

- 应用启动后显示空状态，引导用户选择文件夹。
- 用户可以点击按钮选择一个本地文件夹。
- 应用应记住上次选择的文件夹，下次启动时可自动加载或提供快捷入口。
- 支持手动刷新当前文件夹。

### 2. 扫描视频文件

- 默认递归扫描所选文件夹下的所有子文件夹。
- 支持常见视频格式：
  - `.mp4`
  - `.mkv`
  - `.avi`
  - `.mov`
  - `.wmv`
  - `.flv`
  - `.webm`
  - `.m4v`
- 扫描过程必须异步执行，不能阻塞 UI。
- 支持边扫描边显示结果，不必等全部扫描完成。
- 扫描时显示状态，例如：正在扫描、已发现多少个视频、封面生成进度。
- 如果某些文件无法读取，不应导致程序崩溃，应记录失败并继续处理其他文件。
- 需要支持中文路径、空格路径、长文件名。

### 3. 视频封面缩略图

- 为每个视频生成封面缩略图。
- 默认从视频第 5 秒截取封面；如果视频不足 5 秒，则从视频中间帧截取。
- 使用 ffmpeg 生成缩略图。
- 缩略图需要缓存到本地，避免每次启动重复生成。
- 缓存判断依据至少包含：文件路径、文件大小、修改时间。
- 如果视频文件发生变化，应重新生成封面。
- 封面生成失败时显示统一的占位图，不影响其他视频展示。
- 封面尺寸应适合网格展示，建议宽高比 16:9，图片 `object-fit: cover`。

### 4. 视频信息

每个视频至少提取并展示：

- 文件名
- 文件大小
- 视频时长
- 修改时间

如果实现成本不高，可以额外提取：

- 分辨率
- 视频格式/编码信息

视频信息应缓存，避免重复调用 ffprobe。

### 5. 网格展示

- 主界面采用视频卡片网格布局。
- 每个卡片包含：
  - 视频封面
  - 文件名
  - 文件大小
  - 视频时长
- 长文件名应省略显示，悬停时可查看完整文件名。
- 网格应适配窗口大小。
- 支持调整缩略图大小，可用小/中/大三档。
- 大量视频时要注意性能，尽量使用懒加载或虚拟列表。
- 页面应有清晰的空状态：
  - 未选择文件夹
  - 当前文件夹没有视频
  - 正在扫描

### 5.1 目录筛选

- 左侧显示基于扫描结果生成的文件夹列表。
- 用户点击某个文件夹后，只展示该文件夹及其子文件夹中的视频。
- 文件夹列表应显示每个目录下直接发现的视频数量。
- 支持一键回到“全部视频”。
- 目录筛选不改变文件本身，只影响当前展示列表。

### 5.2 视图模式

- 支持网格视图和列表视图切换。
- 网格视图用于快速看封面。
- 列表视图用于快速扫描文件名、时长、大小、分辨率、修改时间和所在文件夹。
- 两种视图应共享搜索、排序、筛选和右键菜单能力。

### 6. 搜索与排序

- 支持按文件名实时搜索。
- 搜索只影响当前展示列表，不改变文件本身。
- 支持排序：
  - 文件名
  - 修改时间
  - 文件大小
  - 视频时长
- 支持升序/降序切换。
- 默认排序建议为修改时间降序或文件名升序，可根据实现选择一个合理默认值。

### 6.1 筛选条件

- 支持按视频格式筛选，格式选项应基于当前扫描结果动态生成。
- 支持按时长筛选：
  - 全部
  - 1 分钟内
  - 1-20 分钟
  - 20 分钟以上
- 支持按画面方向或分辨率筛选：
  - 全部
  - 横屏
  - 竖屏
  - 方形
  - 720p+
  - 1080p+
  - 4K+
- 支持清除全部筛选条件。

### 7. 右键菜单

对视频卡片点击右键时，显示上下文菜单，至少包含：

- 打开所在目录，并在系统文件管理器中选中该文件
- 用系统默认播放器打开视频
- 复制视频完整路径
- 重新生成封面

Windows 下打开所在目录并选中文件可使用 `explorer /select`。需要注意路径转义，确保中文路径和空格路径正常工作。

### 8. 详情展示

点击某个视频后，在侧边栏或底部区域显示基础详情：

- 完整路径
- 所在文件夹
- 文件大小
- 视频时长
- 修改时间
- 分辨率，如果已提取

详情区域应简洁，不要做成复杂素材管理器。

### 9. UI/UX 要求

- 整体风格应像轻量文件浏览器，而不是视频平台或媒体库。
- 界面应简洁、清晰、稳定。
- 推荐暗色主题，但不要使用过度装饰。
- 顶部工具栏建议包含：
  - 选择文件夹
  - 刷新
  - 搜索框
  - 排序选择
  - 升序/降序按钮
  - 缩略图大小切换
- 底部或顶部显示状态信息：
  - 当前文件夹路径
  - 视频总数
  - 扫描状态
  - 失败数量
- 不要加入标签、收藏、评论、复杂分类、内置播放器等超出范围的功能。

### 10. 性能与稳定性

- 文件扫描、ffprobe、ffmpeg 封面生成都必须异步处理。
- 封面生成应使用任务队列并限制并发，避免 CPU 瞬间占满。
- UI 不应因为大量文件而明显卡顿。
- 缩略图和视频信息应缓存。
- 程序应能处理几百到几千个视频文件。
- 对异常情况要有容错：
  - 文件被删除
  - 文件正在占用
  - ffmpeg 执行失败
  - 路径权限不足
  - 视频文件损坏
  - 网络盘或移动硬盘断开

## 非目标功能

请不要实现以下功能，除非后续明确要求：

- 内置视频播放器
- 删除、移动、重命名视频
- 标签系统
- 收藏系统
- 批量管理
- 重复文件检测
- 云同步
- 登录系统
- 在线视频解析
- 复杂数据库后台服务

## 建议的数据结构

```ts
type VideoItem = {
  id: string;
  filePath: string;
  fileName: string;
  directory: string;
  extension: string;
  size: number;
  modifiedAt: number;
  duration?: number;
  width?: number;
  height?: number;
  thumbnailPath?: string;
  thumbnailStatus: "pending" | "ready" | "failed";
  metadataStatus: "pending" | "ready" | "failed";
  error?: string;
};
```

## 缓存建议

- 使用应用数据目录下的 `cache` 文件夹保存缩略图。
- 使用 JSON 或 SQLite 保存视频元信息缓存。
- 初版可以使用 JSON；如果已有数据库方案，也可以使用 SQLite。
- 缓存 key 可以基于文件绝对路径 + 文件大小 + 修改时间生成 hash。

## 实现顺序建议

1. 搭建 Electron + React + TypeScript 基础项目。
2. 实现选择文件夹和主进程 IPC。
3. 实现递归扫描视频文件。
4. 实现网格 UI 展示文件名、大小、修改时间。
5. 接入 ffprobe 获取时长和分辨率。
6. 接入 ffmpeg 生成封面。
7. 实现缩略图缓存。
8. 实现搜索、排序、缩略图大小切换。
9. 实现右键菜单：打开所在目录、打开视频、复制路径、重新生成封面。
10. 实现详情面板、状态栏、错误提示。
11. 做性能优化和异常处理。

## 验收标准

- 用户可以选择一个本地文件夹。
- 程序能递归发现其中的视频文件。
- 视频以网格卡片展示。
- 每个视频能显示封面、文件名、大小、时长。
- 右键可以打开所在目录并选中文件。
- 右键可以用系统默认播放器打开视频。
- 可以按文件名搜索。
- 可以按名称、修改时间、大小、时长排序。
- 可以通过左侧文件夹列表筛选某个子目录及其子目录下的视频。
- 可以在网格视图和列表视图之间切换。
- 可以按格式、时长、画面方向或分辨率筛选。
- 关闭后重新打开，能复用已生成的封面缓存。
- 部分视频读取失败时，程序不会崩溃。
- 中文路径和中文文件名可以正常处理。
- UI 简洁稳定，符合轻量视频文件浏览器定位。
