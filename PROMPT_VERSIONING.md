# Prompt 版本管理

本项目后续的开发提示词按版本维护，避免把已经实现的功能反复写成待开发任务。每一版 prompt 都必须明确区分“已实现”“本版只做”“本版不做”。

## 对话工作规则

- 本对话默认只做规划、需求拆解和 prompt 编写。
- 不直接修改实现代码。
- 只有当用户明确说“现在开始开发”“开始实现”“改代码”等类似指令时，才进入开发执行。
- 每次写新 prompt 时，都要先确认它是增量版本，而不是重新生成全量开发需求。

## 建议文件组织

```text
prompts/
  01-core-video-browser.md
  02-browser-efficiency.md
  03-ffmpeg-readiness-and-polish.md
  04-release-hardening.md
  05-ui-tree-and-quick-access.md
  06-hover-frame-preview.md
```

当前已有的 `VIDEO_BROWSER_PROMPT.md` 可以作为 v0.1 到 v0.2 的综合背景文档。后续新增 prompt 应优先使用独立版本文件，避免不断膨胀同一个总提示词。

## 版本 Prompt 固定结构

每个版本 prompt 建议使用以下结构：

```md
# 版本名称

## 背景

## 当前已实现功能

## 本版本目标

## 本版本只做

## 本版本不做

## 涉及模块建议

## 实现要求

## 验收标准

## 回归检查
```

其中最重要的是：

- “当前已实现功能”：明确告诉开发代理不要重复实现。
- “本版本只做”：控制范围。
- “本版本不做”：防止功能蔓延。
- “回归检查”：确保旧功能不被破坏。

## 已实现功能基线

截至当前规划，项目应被视为已经具备以下功能。后续 prompt 不要把这些当成从零开发任务：

- 选择本地文件夹。
- 记住上次选择的文件夹。
- 递归扫描视频文件。
- 识别常见视频格式：`.mp4`、`.mkv`、`.avi`、`.mov`、`.wmv`、`.flv`、`.webm`、`.m4v`。
- 生成视频封面缩略图。
- 缓存视频封面和基础元信息。
- 网格展示视频封面、文件名、大小、时长、修改时间。
- 支持搜索文件名。
- 支持按文件名、修改时间、文件大小、时长排序。
- 支持升序/降序切换。
- 支持缩略图小/中/大切换。
- 支持右键菜单：
  - 打开所在目录
  - 用系统默认播放器打开
  - 复制完整路径
  - 重新生成封面
- 支持右侧详情面板。
- 支持底部状态栏。
- 支持左侧文件夹筛选。
- 支持网格视图和列表视图切换。
- 支持按格式、时长、画面方向/分辨率筛选。

## 下一版建议 Prompt：v0.3 浏览效率与 ffmpeg 可用性增强

```text
你是一名资深 Electron + React + TypeScript 桌面应用开发工程师。请在现有 Vid Folder Browser 项目上做增量开发。

重要规则：
- 不要从零重建项目。
- 不要重复实现已经存在的核心功能。
- 优先沿用现有代码结构、IPC 设计、样式系统和数据结构。
- 本版本只做浏览效率打磨和 ffmpeg/ffprobe 可用性检测。
- 不要加入内置播放器、标签、收藏、删除、移动、重命名、批量管理、云同步、登录等功能。

项目定位：
Vid Folder Browser 是一个轻量级本地视频文件浏览器。它用于快速浏览本地文件夹中的视频，通过封面和基础信息帮助用户找到目标视频。它不是视频播放器，也不是复杂素材管理器。

当前已实现功能：
- 选择本地文件夹，并记住上次选择路径。
- 递归扫描视频文件。
- 生成和缓存视频封面。
- 缓存基础视频元信息。
- 网格展示视频封面、文件名、大小、时长、修改时间。
- 搜索文件名。
- 按文件名、修改时间、大小、时长排序。
- 升序/降序切换。
- 缩略图小/中/大切换。
- 右键打开所在目录、用系统默认播放器打开、复制路径、重新生成封面。
- 右侧详情面板。
- 底部状态栏。
- 左侧文件夹筛选。
- 网格视图 / 列表视图切换。
- 按格式、时长、画面方向/分辨率筛选。

本版本目标：
提升日常浏览体验，并降低 ffmpeg/ffprobe 依赖导致的使用门槛。

本版本只做：

1. 设置持久化
- 记住用户上次使用的视图模式：网格 / 列表。
- 记住排序字段和升序/降序。
- 记住缩略图大小。
- 可以考虑记住筛选条件，但如果会造成启动后用户困惑，应优先只记住视图、排序、缩略图大小。
- 设置应保存到现有应用设置文件中，避免新增复杂数据库。

2. 目录栏累计数量
- 当前目录栏显示每个目录直接包含的视频数量。
- 改进为显示该目录及其全部子目录中的视频累计数量。
- “全部视频”仍显示总视频数。
- 点击目录后的筛选行为保持不变：展示该目录及子目录中的视频。

3. 列表视图表头排序
- 在列表视图中，点击表头字段可以切换排序字段。
- 再次点击同一字段切换升序/降序。
- 表头应能显示当前排序方向。
- 至少支持：文件名、时长、大小、修改时间。
- 分辨率和所在文件夹可以先不支持排序，除非实现成本很低。

4. ffmpeg/ffprobe 可用性检测
- 应用启动时检测 `ffmpeg` 和 `ffprobe` 是否可执行。
- 检测结果通过 IPC 提供给前端。
- 如果缺少 `ffmpeg` 或 `ffprobe`，UI 应显示清晰提示。
- 缺少依赖时，不应导致应用崩溃。
- 如果缺少 `ffprobe`，视频时长/分辨率读取可能失败，应提示原因。
- 如果缺少 `ffmpeg`，封面生成可能失败，应提示原因。
- 已有缓存的封面和元信息仍可展示，不要因为检测失败而阻止用户浏览文件列表。

5. 依赖状态展示
- 在状态栏或顶部区域显示简洁状态，例如：
  - `ffmpeg 正常`
  - `ffprobe 正常`
  - `缺少 ffmpeg`
  - `缺少 ffprobe`
- 提示文案应面向普通用户，避免只显示技术错误堆栈。
- README 中补充依赖检测说明。

本版本不做：
- 不内置 ffmpeg。
- 不自动下载 ffmpeg。
- 不做设置页面。
- 不做内置视频播放器。
- 不做删除、移动、重命名。
- 不做标签、收藏、批量管理。
- 不做数据库迁移，除非现有设置文件无法满足。

涉及模块建议：
- 共享类型：扩展设置类型和 ffmpeg 检测结果类型。
- Electron 主进程：读取/保存设置，检测 ffmpeg/ffprobe。
- preload：暴露获取依赖状态、保存设置等 IPC。
- React 前端：恢复设置、响应设置变化、展示依赖状态、列表表头排序。
- README：补充 ffmpeg/ffprobe 检测说明。

实现要求：
- 只做增量修改。
- 保持现有 UI 风格。
- 所有文件路径、中文路径、空格路径行为不得退化。
- ffmpeg/ffprobe 不存在时，应用仍可打开和扫描文件。
- 不能因为依赖缺失导致未捕获异常。
- 设置写入应节流或只在用户变更关键选项时写入，避免频繁写文件。

验收标准：
- 关闭并重新打开应用后，视图模式、排序字段、排序方向、缩略图大小能恢复。
- 左侧目录数量显示为累计数量。
- 列表视图点击“文件名/时长/大小/修改时间”表头可以排序。
- 当前排序字段和方向在列表表头中可见。
- 应用启动后能检测 ffmpeg 和 ffprobe 是否可用。
- 缺少 ffmpeg/ffprobe 时，UI 有明确提示，应用不崩溃。
- 已有封面缓存仍能正常展示。
- 原有功能仍可用：选择文件夹、扫描、网格展示、右键菜单、搜索、筛选、详情面板。

回归检查：
- 能选择文件夹并扫描视频。
- 网格视图仍能显示封面。
- 列表视图仍能显示文件信息。
- 搜索、格式筛选、时长筛选、画面筛选仍能组合使用。
- 右键打开所在目录、打开视频、复制路径、重新生成封面仍工作。
- 中文路径和中文文件名仍正常。
- `npm run typecheck` 通过。
- `npm run build` 通过。
```

## 后续版本候选

### v0.4 发布质量加固

规划复核：OpenAI Codex（GPT-5），2026-08-10。

v0.4 不新增浏览功能，目标是把 v0.3 从“功能可用”推进到“行为可解释、质量可验证、发布可重复”。详细增量 Prompt 见 [`prompts/04-release-hardening.md`](./prompts/04-release-hardening.md)。

本版分为三个阶段：

1. 先定义验证方法和预期行为
   - 建立错误分类与用户文案规则。
   - 建立冷缓存、热缓存大目录基准测试方法。
   - 明确缓存失效、损坏缓存和孤儿缓存的预期处理。
   - 建立可独立执行的发布冒烟清单。
2. 再实施确定性修复
   - 区分元信息读取失败与封面生成失败，避免一个阶段失败阻断另一个阶段。
   - 不再静默吞掉根目录不可读等扫描错误。
   - 用户界面只展示可理解的中文错误，技术详情保留用于诊断。
   - 校验缓存文件是否真实可用，并按已确认策略处理孤儿缓存。
   - 只有实测未达基线时，才做针对性性能优化。
3. 最后完成发布闭环
   - 统一构建资产命名。
   - 更新已知限制、构建和 Release 流程。
   - 通过类型检查、构建、回归、冒烟和发布包核验。

关键范围约束：

- 性能优化必须由测试结果驱动，不在 v0.4 中无依据地重构。
- 不承诺“内容变化但路径、大小、mtime 完全相同”自动失效；默认记录为已知边界，除非测试证明这是高频真实问题。
- 不加入播放器、标签、收藏、文件管理、数据库或自动下载 ffmpeg。
- v0.4 的代码改动只服务于错误处理、缓存可靠性、已证实的性能瓶颈和发布一致性。

### v0.5 界面体系、文件夹树与快速访问

规划与提示词：OpenAI Codex（GPT-5），2026-08-13。

详细增量 Prompt 见 [`prompts/05-ui-tree-and-quick-access.md`](./prompts/05-ui-tree-and-quick-access.md)。

本版本只负责前端体验和导航体系：

- 按官方已有 Vite 项目方式接入 shadcn/ui，只加入实际使用的组件源码。
- 重构前端组件边界和桌面工具布局，保留所有既有业务行为。
- 将扁平目录列表升级为层级文件夹树。
- 增加最近文件夹、固定常用文件夹、失效路径处理和单文件夹拖入打开。
- 增加双击、方向键、Enter、Ctrl+C 等高频浏览操作。
- 记住合理范围内的树展开状态、侧栏和详情栏状态。

本版不做悬停预览、多候选封面、文件管理和完整文件系统树。

### v0.6 悬停多帧预览

规划与提示词：OpenAI Codex（GPT-5），2026-08-13。

详细增量 Prompt 见 [`prompts/06-hover-frame-preview.md`](./prompts/06-hover-frame-preview.md)。

本版本只负责视觉预览链路：

- 网格封面停留后，根据鼠标水平位置查看约 8 个代表帧。
- 预览按需生成，不在普通扫描阶段批量预生成。
- 使用独立缓存、容量上限、近似 LRU 淘汰和安全清理。
- 使用独立有界队列、requestId、取消和陈旧响应隔离。
- 缺少 ffmpeg、短视频、损坏视频和失败任务均回退静态封面。

本版不做播放器、音频、多候选封面选择或保存预览帧为主封面。

### v0.7 以后候选

- 当前文件夹实时变更监控。
- 导出当前结果为 CSV/JSON。
- 编码、码率、帧率和音轨等详情增强。
- 多根目录聚合浏览。
- 自动更新检查。
- 内置 ffmpeg 的许可、包体和更新策略研究。
